from django.urls import path


app_name = "product"
urlpatterns = [
    # path('shop/', ArticlesListView.as_view(), name="list_view"),
    # path('shop/<slug:slug>', ArticleDetailView.as_view(), name='detail_view'),
]